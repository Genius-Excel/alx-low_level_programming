#include "main.h"

/**
* _abs - outputs absolute value
* @n: value to be calculated
* Return: absolute value
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
