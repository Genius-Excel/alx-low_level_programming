#include <stdio.h>

/**
* main - this function prints the name of the file compiled from
* Return: the name of the file which it was compiled from
*/

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
