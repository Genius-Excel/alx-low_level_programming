#include <stdio.h>

/**
* main - this is the main function
*
* Return:always 0
*/

int main(void)
{
	int alpha;

	for (alpha = 0; alpha < 26; alpha++)
	{
		putchar('a' + alpha);
	}

	putchar('\n')
	return (0);
}
