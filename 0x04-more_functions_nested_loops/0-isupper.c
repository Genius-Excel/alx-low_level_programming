#include "main.h"

/**
* _isupper - checks for uppercase
* @c: value to be checked
* Return: 0 if upper, 1 otherwise
*/

int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else if (c == 'a')
	{
		return (0);
	}
}
